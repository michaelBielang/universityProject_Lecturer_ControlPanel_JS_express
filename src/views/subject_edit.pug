extends layout

block style
    include css/home.css

block content
    form(method='POST' action='/subject/update')
        #content
            p.title Fachname ändern
            .tablew
                table.table.table-striped
                    tr
                        th Fachname
                        th Actions
                    tr
                        td
                            input.form-control(type='text' id='subjectName' placeholder=subject.subjectName name='subjectName' required)
                            input(type='hidden' id='subjectId' value=subject.subjectId name='subjectId' required)
                        td
                            div.actionItems
                                button(type='submit' class='btn btn-success')
                                    i.fas.fa-save

